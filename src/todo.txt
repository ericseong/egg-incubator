check if target's systemd service file is the same as the one in rpi dir.
check if session_start.txt is correctly located both for SessionTime and SessionLogger.
order or merge:
1. merge with slow_heating
2. merge with periodic_hen_leaving
3. merge with session_logging 

// eof

